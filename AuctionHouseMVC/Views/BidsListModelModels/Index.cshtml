@model IEnumerable<AuctionHouseMVC.Models.BidsViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Value)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.bidData)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UserName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Value)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.bidData)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UserUrl }) |
            @Html.ActionLink("Details", "Details", new { id=item.UserUrl }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.UserUrl })
        </td>
    </tr>
}

</table>
